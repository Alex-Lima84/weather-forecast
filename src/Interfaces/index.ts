export interface IWeatherApi {
    token: string;
    airQuality: string;
    dataLanguage: string;
    forecastDays: string;
  }

export interface IApiAdvisor {
    token: string;
  }

export interface ISearch {
    searchField: string;
  }

